# Project workflow

After completing any code-change request, run the full project build with:

```powershell
.\gradlew.bat build --console=plain
```

Report the build result before handing the work back.
