package gg.vape.mapping.runtime;

/**
 * Identity class-name remap table for mojmap-name runtimes (NeoForge 1.20.1 /
 * 1.21.1): class names already match the runtime, so remap is a no-op.
 */
public class ClassNameRemapTableIdentity
extends ClassNameRemapTable {
}
